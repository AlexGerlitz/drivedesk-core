name: Public Demo Health

on:
  workflow_dispatch:
  schedule:
    - cron: "17 */6 * * *"

permissions:
  contents: read

jobs:
  pages-demo:
    name: Pages demo health
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - name: Check public demo
        run: |
          set -euo pipefail
          url="https://alexgerlitz.github.io/drivedesk-core/apps/admin/public-demo/"
          base="${url%/}"
          root="${base%/apps/admin/public-demo}"
          html="$(curl -fsSL --retry 3 --retry-delay 5 "$url")"
          printf '%s' "$html" | grep -F "DriveDesk Core Demo"
          printf '%s' "$html" | grep -F "data-demo-api-path"
          printf '%s' "$html" | grep -F "./demo-data.js"
          printf '%s' "$html" | grep -F "./styles.css"
          printf '%s' "$html" | grep -F "./app.js"
          printf '%s' "$html" | grep -F "adapterRows"
          printf '%s' "$html" | grep -F "syncJobRows"
          printf '%s' "$html" | grep -F "integrationHealthRows"
          printf '%s' "$html" | grep -F "alertRoutingSummaryRows"
          printf '%s' "$html" | grep -F "alertBindingRows"
          printf '%s' "$html" | grep -F "incidentSummaryRows"
          printf '%s' "$html" | grep -F "incidentRows"
          printf '%s' "$html" | grep -F "proofSummaryRows"
          printf '%s' "$html" | grep -F "proofGateRows"
          printf '%s' "$html" | grep -F "workflowStageRows"
          printf '%s' "$html" | grep -F "endToEndScenarioRows"
          printf '%s' "$html" | grep -F "workflowTimelineRows"
          printf '%s' "$html" | grep -F "domainEventRows"
          printf '%s' "$html" | grep -F "controlTowerSummaryRows"
          printf '%s' "$html" | grep -F "controlTowerProviderRows"
          printf '%s' "$html" | grep -F "controlTowerContextRows"
          printf '%s' "$html" | grep -F "controlTowerEscalationRows"
          printf '%s' "$html" | grep -F "controlTowerActionPlanRows"
          printf '%s' "$html" | grep -F "controlTowerNotificationRows"
          printf '%s' "$html" | grep -F "controlTowerObservationRows"
          printf '%s' "$html" | grep -F "controlTowerExceptionRows"
          printf '%s' "$html" | grep -F "controlTowerRepairRows"
          printf '%s' "$html" | grep -F "businessIntakeSummaryRows"
          printf '%s' "$html" | grep -F "businessIntakeRows"
          printf '%s' "$html" | grep -F "businessIntakeWorkbenchRows"
          printf '%s' "$html" | grep -F "businessIntakeActionRows"
          printf '%s' "$html" | grep -F "businessIntakeBoundaryRows"
          printf '%s' "$html" | grep -F "businessTaskSummaryRows"
          printf '%s' "$html" | grep -F "businessTaskRows"
          printf '%s' "$html" | grep -F "businessTaskOutboxRows"
          printf '%s' "$html" | grep -F "businessTaskDraftRows"
          printf '%s' "$html" | grep -F "businessTaskBoundaryRows"
          printf '%s' "$html" | grep -F "businessNotificationSummaryRows"
          printf '%s' "$html" | grep -F "businessNotificationChannelRows"
          printf '%s' "$html" | grep -F "businessNotificationDraftRows"
          printf '%s' "$html" | grep -F "businessNotificationBoundaryRows"
          printf '%s' "$html" | grep -F "businessContextSummaryRows"
          printf '%s' "$html" | grep -F "businessContextCardRows"
          printf '%s' "$html" | grep -F "businessContextRuleRows"
          printf '%s' "$html" | grep -F "businessContextActionRows"
          printf '%s' "$html" | grep -F "businessContextBoundaryRows"
          printf '%s' "$html" | grep -F "businessScenarioSummaryRows"
          printf '%s' "$html" | grep -F "businessScenarioRows"
          printf '%s' "$html" | grep -F "businessScenarioFlowRows"
          printf '%s' "$html" | grep -F "businessScenarioDocRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "DRIVEDESK_DEMO_DATA"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "static.fallback"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "/demo/public"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "engineering_70"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "SYSTEM_REVIEW_PATH.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "REVIEWER_QUICKSTART.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "wf-demo-lead-to-student"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "lead.created"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "student.sync.requested"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "endToEndScenario"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "scenario-approval-notification-adapter-incident"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "notification.manager_signature_task.created"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "DriveDeskScheduledValidationMissed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "alert.silence.created"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "incidentResponse"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "incident.resolved"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "postcheck.gates.passed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "file.import.fake"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "crm.bitrix24.mock"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "authProfile"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "oauth2_or_webhook_boundary"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "no_browser_token_storage"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "crm_deal_intake_preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "dead_letter"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "drivedesk_integration_jobs"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "drivedesk_integration_adapter_duration_milliseconds"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessControlTower"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-provider-intake/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_provider_intake.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "raw_provider_payload_not_returned"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "crm_payment_mismatch"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "role_assist"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-workbench-context/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_workbench_context.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "reconcile_crm_payment_status"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "drivedesk_business_exceptions"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "sync_status"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-detections/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-escalations/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-action-plans/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-notifications/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "exception_resolution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "action_plan_updates"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "notification.delivery.requested"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_notification.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "queue_repair_execution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "exception_triage"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "finance_reconciliation"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "payment_reconciliation"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_detection.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-briefings/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "execute_repair_dry_run"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessIntakePipeline"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-intake-pipeline/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_provider_intake.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "external_write_gate"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_INTAKE_PIPELINE.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessTaskHandoff"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-task-handoffs/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_task_handoff.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "internal.noop"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_TASK_HANDOFF.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessNotificationChannels"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-notification-channels/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_notification_channel_matrix.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "requires_private_secret"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_NOTIFICATION_CHANNELS.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessContextAssistant"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-workbench-context/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_workbench_context.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "legal.reference.mock"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_CONTEXT_ASSISTANT.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessActionExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-action-executions/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_action_execution.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "idempotencyKey"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_ACTION_EXECUTION.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessApprovalGateway"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business-approval-gateway/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "business_approval_gateway.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "providerWriteUnlocked"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_APPROVAL_GATEWAY.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "integrationRuntime"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "integration-runtime/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "adapter_runtime.previewed"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "accounting_export_execute"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "INTEGRATION_RUNTIME.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "integrationExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "integration-executions/preview"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "integration_execution.run_ledger_prepared"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "IntegrationReconciliation"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "INTEGRATION_EXECUTION.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "businessScenarioReplay"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "crm-bank-payment-mismatch"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "support-sla-risk"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "procurement-delay-risk"
          curl -fsSL --retry 3 --retry-delay 5 "$base/demo-data.js" | grep -F "BUSINESS_SCENARIO_REPLAY.md"
          curl -fsSL --retry 3 --retry-delay 5 "$base/styles.css" | grep -F "metric-grid"
          curl -fsSL --retry 3 --retry-delay 5 "$base/styles.css" | grep -F "integration-health-grid"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "setupTabs"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "loadApiBackedDemoData"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "demoApi"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "/demo/public"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerProviderRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerContextRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerDetectionRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerEscalationRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerActionPlanRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerNotificationRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "controlTowerBriefingRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessIntakePipeline"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessIntakePipeline"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessTaskHandoff"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessTaskHandoff"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessNotificationChannels"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessNotificationChannels"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessContextAssistant"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessContextAssistant"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessActionExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessActionExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessApprovalGateway"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessApprovalGateway"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillIntegrationRuntime"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "integrationRuntime"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillIntegrationExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "integrationExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessActionExecutionSummaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessActionExecutionPlanRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessActionExecutionPreflightRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessActionExecutionDryRunRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessActionExecutionBoundaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessApprovalGatewaySummaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessApprovalGatewayRequestRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessApprovalGatewayPolicyRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessApprovalGatewayUnlockRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "businessApprovalGatewayBoundaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationRuntimeSummaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationRuntimeStepRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationRuntimePreflightRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationRuntimeOutboxRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationRuntimeBoundaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationExecutionSummaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationExecutionTimelineRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationExecutionRecoveryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/index.html" | grep -F "integrationExecutionBoundaryRows"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessScenarioReplay"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "businessScenarioReplay"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillEngineeringProof"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillAlertRouting"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillIncidentResponse"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillWorkflow"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillEndToEndScenario"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillWorkflowTimeline"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillDomainEvents"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillAdapterContracts"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "auth boundary: "
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "real provider secret"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillIntegrationHealth"
          curl -fsSL --retry 3 --retry-delay 5 "$base/app.js" | grep -F "fillBusinessControlTower"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "public_demo_demo_public_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "synthetic_demo_data"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessControlTower"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessIntakePipeline"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessTaskHandoff"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_task_handoff_demo_demo_business_task_handoff_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/business-task-handoff"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessNotificationChannels"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_notification_channels_demo_demo_business_notification_channels_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/business-notification-channels"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessContextAssistant"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_context_assistant_demo_demo_business_context_assistant_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/business-context-assistant"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessActionExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_action_execution_demo_demo_business_action_execution_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/business-action-execution"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "businessApprovalGateway"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_approval_gateway_demo_demo_business_approval_gateway_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/business-approval-gateway"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "integrationRuntime"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "integration_runtime"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/integration-runtime"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "integrationExecution"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "integration_execution"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "/demo/integration-execution"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/openapi-client-manifest.json" | grep -F "business_scenario_replay_demo_demo_business_scenario_replay_get"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/python/drivedesk_public_demo_client.py" | grep -F "DriveDeskPublicDemoClient"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/python/drivedesk_public_demo_client.py" | grep -F "build_adapter_operation_plan"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/python/drivedesk_public_demo_client.py" | grep -F "get_business_scenario_replay"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/javascript/drivedesk-public-demo-client.mjs" | grep -F "DriveDeskPublicDemoClient"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/javascript/drivedesk-public-demo-client.mjs" | grep -F "buildAdapterOperationPlan"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/javascript/drivedesk-public-demo-client.mjs" | grep -F "getBusinessScenarioReplay"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/typescript/drivedesk-public-demo-client.d.ts" | grep -F "PublicDemoPayload"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/typescript/drivedesk-public-demo-client.d.ts" | grep -F "AdapterOperationPlan"
          curl -fsSL --retry 3 --retry-delay 5 "$root/sdk/generated/public-demo/typescript/drivedesk-public-demo-client.d.ts" | grep -F "BusinessScenarioReplayPayload"
          curl -fsSL --retry 3 --retry-delay 5 "$root/docs/public/SYSTEM_REVIEW_PATH.md" | grep -F "System Review Path"
          curl -fsSL --retry 3 --retry-delay 5 "$root/docs/public/SYSTEM_REVIEW_PATH.md" | grep -F "endToEndScenario"
          curl -fsSL --retry 3 --retry-delay 5 "$root/docs/public/REVIEWER_QUICKSTART.md" | grep -F "45-Minute Deep Check"
