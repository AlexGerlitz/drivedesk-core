{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "drivedesk-core.serviceAccountName" . }}
  labels:
    {{- include "drivedesk-core.labels" . | nindent 4 }}
{{- end }}
